const loginValidator = require('./loginValidator');
const tokenValidator = require('./tokenValidator');
const newTalkerValidator = require('./newTalkValidator');

module.exports = {
  loginValidator,
  tokenValidator,
  newTalkerValidator,
};